现在是词语管理、成语管理两个模块

我想分成三个大模块，顶部横幅每个模块增加下拉选择详细内容：

- 汉字模块（包括字、词、成语）
- 题目模块：包括HSK、YCT两种考试类型、"听力题""阅读题""书写题""写作题"四种题型
- 音视频资源模块：包括音频和视频

注意：

1. 词语、成语相关的后端FastAPI逻辑已经写好，但其他资源目前MySQL中并没有资源，可以先把前端框架搭起来，后续确定了数据库资源再把后端逻辑补上
2. 首页的搜索框前增加双选，例如，先选择“汉字模块”，再选择“成语”。
---

## 前端模块重构规划

### 一、当前现状分析

**现有结构：**
- **导航模块**：首页、成语管理、词语管理
- **路由配置**：
  - `/home` - 首页（大搜索框 + 成语/词语搜索）
  - `/chengyu` - 成语管理列表
  - `/ciyu` - 词语管理列表
- **功能**：成语和词语的增删改查、搜索

**技术栈**：Vue 3 + Vue Router + Pinia + Axios + Vite

---

### 二、目标架构

将系统重构为 **三大模块**，每个模块包含多个子类型：

#### 1️⃣ 汉字模块
- **字**：汉字管理（暂无后端，先搭框架）
- **词**：词语管理（已有后端）
- **成语**：成语管理（已有后端）

#### 2️⃣ 题目模块
- **考试类型**：HSK、YCT
- **题型**：听力题、阅读题、书写题、写作题
- **组合方式**：每种考试类型下包含四种题型

#### 3️⃣ 音视频资源模块
- **音频**：音频资源管理
- **视频**：视频资源管理

---

### 三、具体改造方案

#### 3.1 顶部导航栏改造

**文件**：`Header.vue`

**改造内容**：
- 将平铺的导航链接改为 **模块下拉菜单**
- 每个主模块下显示子选项

**预期结构**：
```
┌─────────────────────────────────────────────────────┐
│ 中文教育资源管理系统     [汉字模块▼] [题目模块▼] [音视频模块▼]  用户信息 │
└─────────────────────────────────────────────────────┘
     [字] [词] [成语]      [HSK-听力] [HSK-阅读]...    [音频] [视频]
```

#### 3.2 首页搜索框改造

**文件**：`Home.vue`

**改造内容**：
- 在搜索框前增加 **二级级联选择**
- 第一级选择模块（汉字/题目/音视频）
- 第二级根据第一级动态显示子类型

**示例交互**：
```
[汉字模块 ▼] -> [字 ▼] [ 输入关键字 ]  [搜索]
             -> [词 ▼]
             -> [成语 ▼]

[题目模块 ▼]  -> [HSK ▼] -> [听力题 ▼]
                       -> [阅读题 ▼]
             -> [YCT ▼]
```

#### 3.3 路由结构重构

**文件**：`router/index.js`

**新路由设计**：

```
/home                    - 首页（搜索入口）
/hanzi/zi               - 汉字管理
/hanzi/ciyu             - 词语管理（原 /ciyu）
/hanzi/chengyu          - 成语管理（原 /chengyu）
/exam/hsk/listening     - HSK听力题
/exam/hsk/reading       - HSK阅读题
/exam/hsk/writing       - HSK书写题
/exam/hsk/essay         - HSK写作题
/exam/yct/listening     - YCT听力题
/exam/yct/reading       - YCT阅读题
/exam/yct/writing       - YCT书写题
/exam/yct/essay         - YCT写作题
/media/audio            - 音频资源
/media/video            - 视频资源
```

#### 3.4 视图组件规划

**新增/改造的视图文件**：

| 模块 | 视图文件 | 状态 |
|------|---------|------|
| 汉字-字 | `views/hanzi/HanziList.vue` | 新建（占位） |
| 汉字-词 | `views/CiyuList.vue` | 复用 |
| 汉字-成语 | `views/ChengyuList.vue` | 复用 |
| 题目模板 | `views/exam/ExamTemplate.vue` | 新建（通用模板） |
| 题目-HSK各题型 | `views/exam/HSK*.vue` | 新建（占位） |
| 题目-YCT各题型 | `views/exam/YCT*.vue` | 新建（占位） |
| 音频资源 | `views/media/AudioList.vue` | 新建（占位） |
| 视频资源 | `views/media/VideoList.vue` | 新建（占位） |

**建议结构**：
```
views/
├── hanzi/
│   └── HanziList.vue      # 新建（汉字管理）
├── exam/
│   ├── ExamTemplate.vue   # 新建（题目通用模板）
│   ├── HSKListening.vue   # 新建
│   ├── HSKReading.vue     # 新建
│   ├── HSKWriting.vue     # 新建
│   ├── HSKEssay.vue       # 新建
│   ├── YCTListening.vue   # 新建
│   ├── YCTReading.vue     # 新建
│   ├── YCTWriting.vue     # 新建
│   └── YCTEssay.vue       # 新建
└── media/
    ├── AudioList.vue      # 新建
    └── VideoList.vue      # 新建
```

#### 3.5 API 请求模块规划

**文件**：`utils/request.js`

**新增 API 接口规划**：
```javascript
// 汉字模块
GET  /api/v1/hanzi/         # 汉字列表（待实现）
POST /api/v1/hanzi/         # 添加汉字
GET  /api/v1/ciyu/          # 词语（已存在）
GET  /api/v1/chengyu/       # 成语（已存在）

// 题目模块（待实现）
GET  /api/v1/questions/hsk/listening
GET  /api/v1/questions/hsk/reading
...等8个接口

// 音视频模块（待实现）
GET  /api/v1/media/audio
GET  /api/v1/media/video
```

#### 3.6 状态管理（Pinia）

**文件**：`stores/`

**建议新增**：
- `stores/module.js` - 模块切换状态管理
- `stores/search.js` - 搜索条件状态管理（模块+类型级联）

---

### 四、实施步骤建议

#### 阶段一：导航栏改造（优先级：高）
1. 修改 `Header.vue`，将导航改为下拉菜单形式
2. 实现模块级联选择效果

#### 阶段二：路由重构（优先级：高）
1. 更新 `router/index.js`，按模块组织路由
2. 确保旧路由兼容或重定向

#### 阶段三：首页搜索框改造（优先级：高）
1. 在 `Home.vue` 实现二级级联选择
2. 调整搜索逻辑，根据模块+类型调用不同接口

#### 阶段四：创建占位页面（优先级：中）
1. 创建汉字管理占位页面
2. 创建题目模块占位页面
3. 创建音视频模块占位页面

#### 阶段五：API 对接（优先级：低）
1. 词语、成语逻辑已可用
2. 其他模块待后端实现后再对接

---

### 五、注意事项

1. **向后兼容**：保持原有成语和词语功能正常运行
2. **渐进式改造**：先搭建框架，再逐步完善功能
3. **代码复用**：题目模块的8个页面可以共用 `ExamTemplate.vue`，只传入不同参数
4. **占位处理**：未实现的模块显示"功能开发中"提示